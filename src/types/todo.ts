export type inputForm = {
  text: string;
  name: string;
  type: string;
  id: string;
  placeholder?: string;
};

// export type buttonForm = {
//   children: React.ReactNode;
// };
export type TodoList = {
  idx: string;
  title: string;
  content: string;
};
